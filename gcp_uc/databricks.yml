# The name of the bundle. run `databricks bundle schema` to see the full bundle settings schema.
bundle:
  # Do not modify the below line, this autogenerated field is used by the Databricks backend.
  uuid: e47ab515-2728-4aed-a7c9-4abef5d7fe3f

  name: gcp_uc

variables:
  experiment_name:
    description: Experiment name for the model training.
    default: /Users/${workspace.current_user.userName}/${bundle.target}-gcp_uc-experiment
  model_name:
    description: Model name for the model training.
    default: gcp_uc-model
  catalog_name:
    description: The catalog name to save the trained model
    
include:
  # Resources folder contains ML artifact resources for the ML project that defines model and experiment
  # And workflows resources for the ML project including model training -> validation -> deployment,
  # batch inference, quality monitoring, metric refresh, alerts and triggering retraining
  - ./resources/batch-inference-workflow-resource.yml
  - ./resources/ml-artifacts-resource.yml
  - ./resources/model-workflow-resource.yml
  # TODO: uncomment once monitoring inference table has been created
  # - ./resources/monitoring-resource.yml

# Deployment Target specific values for workspace
targets:
  dev:  # UC Catalog Name 
    mode: development
    default: true
    variables:
      catalog_name: dev
    workspace:
      # TODO: add dev workspace URL
      host:

  staging:
    variables:
      catalog_name: eric_staging
    workspace:
      host: https://416411475796958.8.gcp.databricks.com

  prod:
    variables:
      catalog_name: eric_prod
    workspace:
      host: https://416411475796958.8.gcp.databricks.com

  test:
    variables:
      catalog_name: eric_test
    workspace:
      host: https://416411475796958.8.gcp.databricks.com
